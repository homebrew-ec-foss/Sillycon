This week, I completed the following tasks using verilog:
- Center pix_x/pix_y around screen middle using signed subtraction and verify by coloring left/right halves differently
- Mirror coordinates across one axis (sign-check + conditional negate), then a second axis
- Detect a single clean button press on ui_in[0] (edge detection, not level) and toggle something visible
- Also revised parameters and arrays in verilog.
- Found some design inspirations for creating a kaleidoscopic visual.
